# 0211

 ## 파일 업로드
```bash
multipart : 기존(텍스트, 문자열)과 다른 종류의 파일(사진, 영상 등등)을 주고 받기 위한 방식
=> 텍스트와 파일을 한 바구니에 담아 보내기 위해 구역을 나눈 데이터 형식
```

 ## 프런트>백 전달 방식
- formdata / json 형식
```bash
app2-event1, app2-event2 참고
용량적으로는 json이 더 커서 불리하나 상황에 따라 써야할 수 있다.
ex. 백에서 무조건 json형식만 쓸 때
폼데이터로 받고 백에서 base64형식으로 변환도 가능
```
